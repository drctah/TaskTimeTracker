<Window x:Class="TaskTimeTracker.Client.Ui.Inbox.Inbox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" Title="Inbox" Height="105" Width="300"
        Loaded="Inbox_OnLoaded">
  <StackPanel Margin="5">
    <TextBlock>Text:</TextBlock>
    <TextBox x:Name="TextBox" Height="20" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" KeyUp="OnKeyUp"></TextBox>
    <Grid Margin="0 5 0 0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"></ColumnDefinition>
        <ColumnDefinition Width="50"></ColumnDefinition>
        <ColumnDefinition Width="5"></ColumnDefinition>
        <ColumnDefinition Width="50"></ColumnDefinition>
      </Grid.ColumnDefinitions>
      <Button Grid.Column="1"  Content="Ok" Command="{Binding OkCommand}"></Button>
      <Button Grid.Column="3" Content="Abort" Command="{Binding AbortCommand}"></Button>
    </Grid>
  </StackPanel>
</Window>
