<Window x:Class="TaskTimeTracker.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:client="clr-namespace:TaskTimeTracker.Client"
        mc:Ignorable="d" Title="MainWindow" Height="350" Width="525">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <ListBox Grid.Column="0" ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}">
      <ListBox.ItemTemplate>
        <DataTemplate DataType="client:Task">
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding CreationTime, StringFormat=HH:mm:ss}"/>
            <TextBlock Text=" - "/>
            <TextBlock Text="{Binding Tag}" />
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
    <StackPanel Grid.Column="1">
      <Button Content="Add" Command="{Binding AddCommand}"></Button>
      <Button Content="Remove" Command="{Binding RemoveCommand}"></Button>
    </StackPanel>
  </Grid>
</Window>
